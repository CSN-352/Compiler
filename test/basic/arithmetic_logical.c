int main() {
    int a = 10, b = 5;
    int sum = a + b;
    int diff = a - b;
    int prod = a * b;
    int quot = a / b;
    int mod = a % b;
    int result = (a > b) && (b < 15) || !(a == b);
    int complea = (a + b) * (a - b) / 2;
    int logic = (a > b) && (b != 0) || (a < 50);
    float a = 5.5, b = 2.2;
    float prod = a * b;
    float quot = a / b;
    char ch = 'A';
    char next_ch = ch + 1;
} 