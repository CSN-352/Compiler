// Passing
int x;

double main() {
    int a = 10, b = 5;
    int sum = (a + b);
    int diff = a - b;
    int prod = a * b;
    int quot = a / b;
    int mod = a % b;
    int result = (((a > b) && (b < 15)) || !(a == b));
    int complea = (a + b) * (a - b) / 2;
    int logic = (a > b) && (b != 0) || (a < 50);
    float c = 5.5, d = 2.2;
    float f_prod = c * d;
    float f_quot = c / d;
    char ch = 'A';
    char next_ch = ch + 1;
    // breakpoint;
    // breakpoint_global;
}

int y;
